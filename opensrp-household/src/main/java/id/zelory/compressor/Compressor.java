package id.zelory.compressor;

import android.content.Context;

/**
 * Minimal stub of the Compressor class to satisfy references in FamilyLibrary.
 * Replace with the real library if image compression is required at runtime.
 */
public class Compressor {
    public Compressor(Context context) {
        // no-op stub
    }
}

